import React from 'react';
interface Props {
    threshold?: number;
    ref?: React.MutableRefObject<HTMLDivElement>;
    rootMargin?: string;
}
export declare const useIntersected: ({ threshold, ref, rootMargin }: Props) => boolean[];
export {};
